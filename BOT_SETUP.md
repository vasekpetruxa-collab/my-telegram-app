# 🤖 Настройка Telegram бота

Это руководство поможет вам настроить и запустить Telegram бота для обработки заказов из Web App.

## 📋 Шаг 1: Создание бота в Telegram

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и придумайте имя и username для бота
4. **Сохраните токен бота** - он понадобится в следующем шаге

## 📋 Шаг 2: Настройка Web App

1. Отправьте BotFather команду `/newapp`
2. Выберите вашего бота
3. Укажите название приложения (например, "Меню ресторана")
4. Загрузите иконку (опционально)
5. Укажите URL вашего Web App (где размещен `index.html`)
   - Для разработки можно использовать: `https://your-domain.com` или локальный туннель (ngrok)
6. Сохраните полученный URL

## 📋 Шаг 3: Установка зависимостей

Убедитесь, что у вас установлен Node.js (версия 14 или выше).

```bash
npm install
```

## 📋 Шаг 4: Настройка переменных окружения

1. Скопируйте файл `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Откройте `.env` и заполните:
   - `BOT_TOKEN` - токен бота от BotFather
   - `WEB_APP_URL` - URL вашего Web App
   - `ADMIN_IDS` - ваш Telegram ID (узнать можно у [@userinfobot](https://t.me/userinfobot))

Пример:
```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
WEB_APP_URL=https://my-restaurant.com
ADMIN_IDS=123456789
```

## 📋 Шаг 5: Запуск бота

### Обычный запуск:
```bash
npm start
```

### Запуск с автоперезагрузкой (для разработки):
```bash
npm run dev
```

Если все настроено правильно, вы увидите сообщение:
```
🤖 Бот запущен и готов к работе!
📱 Web App URL: https://your-domain.com
```

## 🎯 Как это работает

1. Пользователь нажимает кнопку "Открыть меню" в боте
2. Открывается Web App (ваш `index.html`)
3. Пользователь выбирает блюда и оформляет заказ
4. Данные отправляются в бота через `tg.sendData()`
5. Бот обрабатывает заказ и отправляет подтверждение
6. Администраторы получают уведомление о новом заказе

## 🔧 Добавление новых функций

### Добавление новой команды

Откройте `bot.js` и добавьте новую команду:

```javascript
bot.command('mycommand', async (ctx) => {
    await ctx.reply('Привет! Это моя новая команда!');
});
```

### Обработка текстовых сообщений

```javascript
bot.on('text', async (ctx) => {
    const text = ctx.message.text;
    
    if (text.includes('привет')) {
        await ctx.reply('Привет! Чем могу помочь?');
    }
});
```

### Добавление кнопок

```javascript
bot.command('menu', async (ctx) => {
    await ctx.reply('Выберите действие:', {
        reply_markup: {
            inline_keyboard: [
                [
                    { text: '🍕 Меню', web_app: { url: process.env.WEB_APP_URL } },
                    { text: '📞 Контакты', callback_data: 'contacts' }
                ],
                [
                    { text: 'ℹ️ О нас', callback_data: 'about' }
                ]
            ]
        }
    });
});

// Обработка нажатий на кнопки
bot.action('contacts', async (ctx) => {
    await ctx.answerCbQuery();
    await ctx.reply('📞 Наши контакты:\nТелефон: +7 (999) 123-45-67');
});
```

### Интеграция с базой данных

Установите библиотеку для работы с БД (например, `mongoose` для MongoDB):

```bash
npm install mongoose
```

Затем в `bot.js`:

```javascript
const mongoose = require('mongoose');

// Подключение к БД
mongoose.connect(process.env.MONGODB_URI);

// Схема заказа
const orderSchema = new mongoose.Schema({
    orderId: String,
    items: Array,
    total: Number,
    phone: String,
    // ... другие поля
});

const Order = mongoose.model('Order', orderSchema);

// Сохранение заказа
bot.on('message', async (ctx) => {
    const webAppData = ctx.message?.web_app?.data;
    if (webAppData) {
        const orderData = JSON.parse(webAppData);
        const order = new Order(orderData);
        await order.save();
    }
});
```

### Интеграция с CRM

Добавьте функцию отправки заказа в CRM:

```javascript
async function sendOrderToCRM(order) {
    const response = await fetch('https://your-crm-api.com/orders', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'Authorization': `Bearer ${process.env.CRM_API_KEY}`
        },
        body: JSON.stringify(order)
    });
    
    return response.json();
}

// Использование в обработчике заказа
bot.on('message', async (ctx) => {
    // ... обработка заказа
    await sendOrderToCRM(order);
});
```

## 📚 Полезные ресурсы

- [Документация Telegraf](https://telegraf.js.org/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram Web App API](https://core.telegram.org/bots/webapps)

## 🐛 Решение проблем

### Бот не отвечает
- Проверьте, что токен бота правильный
- Убедитесь, что бот запущен (`npm start`)
- Проверьте логи на наличие ошибок

### Web App не открывается
- Проверьте, что URL в `.env` правильный
- Убедитесь, что сайт доступен по HTTPS
- Проверьте настройки Web App в BotFather

### Заказы не обрабатываются
- Проверьте, что данные отправляются из `app.js` через `tg.sendData()`
- Убедитесь, что обработчик `bot.on('message')` работает
- Проверьте логи на наличие ошибок парсинга JSON

## 📞 Поддержка

Если у вас возникли проблемы, проверьте:
1. Логи бота в консоли
2. Правильность настройки переменных окружения
3. Доступность Web App по указанному URL

