# 🔍 Диагностика: Бот не отвечает на команды

## ❌ Проблема:
Бот в Telegram не отвечает на команды (`/start`, `/help`, `/stats`, `/orders`, `/allorders`).

## ✅ Решение:

### Шаг 1: Проверьте, запущен ли бот

В PowerShell выполните:
```powershell
Get-Process node -ErrorAction SilentlyContinue | Where-Object {$_.Path -notlike "*cursor*"}
```

**Должен быть ОДИН процесс Node.js** (кроме процессов Cursor).

### Шаг 2: Запустите бота с логами

```powershell
cd d:\my-telegram-app
node bot.js
```

### Шаг 3: Отправьте команду в боте

В Telegram откройте бота `@bunkerREST_bot` и отправьте команду `/start` или `/help`.

### Шаг 4: Проверьте логи в консоли

**Если команда работает, вы увидите:**
```
🔔 ПОЛУЧЕНО ОБНОВЛЕНИЕ
Тип обновления: message
📨 Это сообщение
Текст: /start
isCommand: true
✅ Команда /start получена от пользователя: 540298072
✅ Ответ на /start отправлен
```

**Если команда НЕ работает, вы НЕ увидите:**
- `✅ Команда /start получена`
- `✅ Ответ на /start отправлен`

## 🔍 Возможные причины:

### 1. Бот не запущен
**Решение:** Запустите бота (`node bot.js`)

### 2. Несколько экземпляров бота
**Решение:** Остановите все процессы Node.js и запустите один раз

### 3. Бот не получает обновления
**Решение:** Проверьте интернет-соединение и токен бота

### 4. Ошибка в коде
**Решение:** Проверьте логи на наличие ошибок

## 📋 Команды для проверки:

1. **Остановить все процессы:**
   ```powershell
   Get-Process node -ErrorAction SilentlyContinue | Where-Object {$_.Path -notlike "*cursor*"} | Stop-Process -Force
   ```

2. **Запустить бота:**
   ```powershell
   cd d:\my-telegram-app
   node bot.js
   ```

3. **Проверить логи:**
   - Отправьте команду в боте
   - Смотрите логи в консоли

## ✅ Что должно быть в логах:

При отправке команды `/start`:
```
🔔 ПОЛУЧЕНО ОБНОВЛЕНИЕ
Тип обновления: message
📨 Это сообщение
Текст: /start
isCommand: true
✅ Команда /start получена от пользователя: [ID]
✅ Ответ на /start отправлен
```

## 🚨 Если логи не появляются:

1. **Проверьте, что бот запущен**
2. **Проверьте интернет-соединение**
3. **Проверьте токен бота в `.env`**
4. **Перезапустите бота**

