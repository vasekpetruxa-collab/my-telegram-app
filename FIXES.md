# 🔧 Исправления ошибок при оформлении заказа

## ❌ Найденные ошибки:

### 1. `Method requestContact is not supported in version 6.0`
**Проблема:** Telegram Web App версии 6.0+ не поддерживает метод `requestContact`
**Исправление:** Убрано использование `requestContact`, теперь пользователь вводит номер телефона вручную

### 2. CORS ошибка при запросе к Nominatim API
**Проблема:** 
- `Access to fetch at 'https://nominatim.openstreetmap.org/...' has been blocked by CORS policy`
- `GET https://nominatim.openstreetmap.org/... net::ERR_FAILED 403 (Forbidden)`

**Причина:** Nominatim API блокирует прямые запросы из браузера из-за политики CORS

**Исправление:** Отключен автопоиск адресов через Nominatim API. Пользователь должен вводить адрес доставки вручную.

## ✅ Что исправлено:

1. **Функция `requestPhoneNumber()`:**
   - Убрано использование `tg.requestContact()`
   - Теперь просто фокусируется на поле ввода телефона
   - Показывает сообщение "Введите номер телефона вручную"

2. **Функция `searchAddress()`:**
   - Убраны запросы к Nominatim API
   - Показывает сообщение "Введите адрес доставки вручную"
   - Не вызывает CORS ошибок

## 📝 Текущее поведение:

### Ввод телефона:
- Пользователь нажимает кнопку "Запросить номер" (если есть)
- Поле ввода телефона получает фокус
- Пользователь вводит номер вручную

### Ввод адреса:
- Пользователь выбирает "Доставка"
- Вводит адрес вручную в поле "Адрес доставки"
- Кнопка "Найти адрес" больше не делает запросы к API
- Пользователь может ввести адрес полностью вручную

## 🚀 Результат:

- ✅ Нет ошибок в консоли
- ✅ Оформление заказа работает без проблем
- ✅ Пользователь может ввести все данные вручную
- ✅ Заказ успешно отправляется в бота

## 💡 Альтернативные решения (на будущее):

Если нужно восстановить автопоиск адресов:
1. Использовать прокси-сервер для запросов к Nominatim API
2. Использовать другой API для поиска адресов (например, Яндекс.Карты API)
3. Реализовать поиск адресов на серверной стороне (через бота)

