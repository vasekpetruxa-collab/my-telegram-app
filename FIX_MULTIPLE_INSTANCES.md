# ⚠️ Ошибка 409: Conflict - несколько экземпляров бота

## ❌ Проблема:
```
TelegramError: 409: Conflict: terminated by other getUpdates request; 
make sure that only one bot instance is running
```

## 🔍 Причина:
Запущено **несколько экземпляров бота одновременно**. Telegram API не позволяет нескольким экземплярам одного бота получать обновления одновременно.

## ✅ Решение:

### Шаг 1: Остановить все процессы Node.js
В PowerShell выполните:
```powershell
Get-Process node -ErrorAction SilentlyContinue | Where-Object {$_.Path -notlike "*cursor*"} | Stop-Process -Force
```

Или проще:
```powershell
Get-Process node | Stop-Process -Force
```

### Шаг 2: Подождать 2-3 секунды
```powershell
Start-Sleep -Seconds 3
```

### Шаг 3: Проверить, что все процессы остановлены
```powershell
Get-Process node -ErrorAction SilentlyContinue
```
Должно быть пусто (или только процессы Cursor).

### Шаг 4: Запустить бота заново (ТОЛЬКО ОДИН РАЗ!)
```powershell
cd d:\my-telegram-app
node bot.js
```

## 🚨 Важно:

- **Запускайте бота ТОЛЬКО ОДИН РАЗ**
- Если запустили в одном окне - не запускайте в другом
- Если бот уже запущен - не запускайте снова

## 🔍 Как проверить, что бот запущен:

```powershell
Get-Process node -ErrorAction SilentlyContinue | Where-Object {$_.Path -notlike "*cursor*"}
```

Должен быть **только один** процесс Node.js (кроме процессов Cursor).

## 💡 Почему это происходит:

1. Запустили бота в одном окне
2. Запустили бота в другом окне (или через npm start)
3. Оба экземпляра пытаются получать обновления от Telegram
4. Telegram API выдает ошибку 409 Conflict

## ✅ Правильная последовательность:

1. **Остановите все процессы Node.js**
2. **Запустите бота ОДИН РАЗ** в одном окне
3. **Оставьте это окно открытым**
4. **Не запускайте бота в других окнах**

