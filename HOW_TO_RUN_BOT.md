# 🚀 Как правильно запустить бота

## ❓ Почему "ничего не происходит" после `node bot.js`?

**Это нормально!** Бот - это долго работающий процесс. Когда он запускается успешно:
- Он не завершается сразу
- Он продолжает работать и ждать сообщений
- Вывод может появиться не сразу (если есть задержка подключения к Telegram API)

## ✅ Как проверить, что бот запустился:

### Способ 1: Проверить процессы
В **новом** окне PowerShell выполните:
```powershell
Get-Process node | Where-Object {$_.Path -notlike "*cursor*"}
```
Если видите процесс Node.js - бот запущен!

### Способ 2: Проверить в Telegram
1. Откройте Telegram
2. Найдите бота: `@bunkerREST_bot`
3. Отправьте `/start`
4. Если бот отвечает - он работает!

### Способ 3: Подождать вывод
После `node bot.js` подождите 2-3 секунды. Должны появиться сообщения:
```
🚀 Начало загрузки бота...
✅ Telegraf загружен
✅ .env загружен
🔧 Инициализация бота...
✅ Бот инициализирован
🤖 Бот запущен и готов к работе!
📱 Web App URL: ...
```

## 🔍 Если вывод не появляется:

### Возможные причины:
1. **Бот запускается, но вывод не виден** - подождите несколько секунд
2. **Ошибка при запуске** - проверьте, что `.env` файл существует и содержит правильный токен
3. **Проблема с подключением** - проверьте интернет-соединение

### Как увидеть ошибки:
Запустите бота и подождите 5-10 секунд. Если есть ошибки, они появятся в консоли.

## 📝 Правильная последовательность:

1. Откройте PowerShell
2. Перейдите в папку проекта:
   ```powershell
   cd d:\my-telegram-app
   ```
3. Запустите бота:
   ```powershell
   node bot.js
   ```
4. **Подождите 2-3 секунды** - должны появиться сообщения о запуске
5. Если сообщения появились - бот работает! ✅
6. Оставьте окно открытым - бот должен работать постоянно

## ⚠️ Важно:

- **Не закрывайте окно терминала** - бот работает в этом окне
- **Если закрыть окно** - бот остановится
- **Для постоянной работы** используйте фоновый режим или сервис

## 🛑 Остановка бота:

Нажмите `Ctrl+C` в окне терминала, где запущен бот.

