# 🔧 Настройка Menu Button для команды /start

## Что нужно сделать

Menu Button в Telegram должен вызывать команду `/start` в боте (показывать диалог с кнопками).

## Шаг 1: Настройка Menu Button в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота `@bunkerREST_bot`
4. Выберите `Bot Settings`
5. Выберите `Menu Button`
6. Нажмите `Configure Menu Button`
7. Выберите `Bot Commands` (команды бота)
8. Выберите команду `/start`
9. Нажмите `Done`

**Альтернативный способ (если Bot Commands не работает):**
- Настройте Menu Button на команду через `/setmenubutton`:
  ```
  /setmenubutton
  @bunkerREST_bot
  /start
  ```

## Шаг 2: Проверка работы

1. Откройте бота в Telegram
2. В главном меню (список чатов) найдите вашего бота
3. Рядом с ботом должна быть кнопка "Открыть" (или текст, который вы указали)
4. Нажмите на эту кнопку
5. Должен открыться диалог с кнопками (команда `/start`)

## Как это работает

- При нажатии кнопки "Открыть" в главном меню Telegram вызывается команда `/start`
- Пользователь видит приветственное сообщение с кнопками (Меню, Мои заказы, Статус заказа, Помощь)
- Кнопка "Меню" в Reply Keyboard открывает Web App для создания заказа
- Заказы обрабатываются через `web_app_data` при оформлении через кнопку "Меню"

## Важно

- Menu Button вызывает команду `/start` (показывает диалог с кнопками)
- Кнопка "Меню" в Reply Keyboard открывает Web App для создания заказа
- Заказы обрабатываются **ТОЛЬКО** через `web_app_data` (кнопка "Меню" в панели)

