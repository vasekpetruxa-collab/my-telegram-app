# 🔧 Настройка Menu Button для команды /start

## Что нужно сделать

Menu Button (кнопка "Открыть" в главном меню Telegram) должен вызывать команду `/start`, которая показывает диалог с кнопками.

## Шаг 1: Настройка Menu Button в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота `@bunkerREST_bot`
4. Выберите `Bot Settings`
5. Выберите `Menu Button`
6. Нажмите `Configure Menu Button`
7. Введите:
   - **Text**: `Открыть` (или любой другой текст)
   - **URL**: `https://vasekpetruxa-collab.github.io/my-telegram-app/start.html`
8. Нажмите `Done`

**Важно:** URL должен указывать на файл `start.html`, который автоматически вызывает команду `/start` в боте.

## Шаг 2: Загрузите start.html на GitHub Pages

Убедитесь, что файл `start.html` загружен на GitHub Pages вместе с остальными файлами.

## Шаг 3: Проверка работы

1. Откройте бота в Telegram
2. В главном меню (список чатов) найдите вашего бота
3. Рядом с ботом должна быть кнопка "Открыть" (или текст, который вы указали)
4. Нажмите на эту кнопку
5. Откроется Web App (`start.html`), который автоматически вызовет команду `/start`
6. Web App закроется, и вы увидите диалог с кнопками от бота

## Как это работает

### Menu Button (кнопка "Открыть" в главном меню):
- При нажатии вызывается команда `/start`
- Пользователь видит приветственное сообщение с кнопками:
  - 🍕 Меню
  - 📋 Мои заказы
  - 📊 Статус заказа
  - ℹ️ Помощь

### Кнопка "Меню" в Reply Keyboard:
- При нажатии открывается Web App для создания заказа
- Пользователь может выбрать блюда и оформить заказ
- Заказ отправляется через `tg.sendData()` и обрабатывается ботом

## Важно

- Menu Button вызывает команду `/start` (показывает диалог с кнопками)
- Кнопка "Меню" в Reply Keyboard открывает Web App для создания заказа
- Заказы обрабатываются **ТОЛЬКО** через `web_app_data` (кнопка "Меню" в панели)

## Как это работает

1. Пользователь нажимает кнопку "Открыть" в главном меню Telegram
2. Открывается Web App (`start.html`)
3. `start.html` автоматически отправляет команду `/start` боту через Telegram Bot API
4. Бот получает команду `/start` и показывает диалог с кнопками
5. Web App закрывается, пользователь видит сообщение от бота с кнопками

## Важно

- URL Menu Button должен указывать на `start.html`
- `start.html` автоматически вызывает команду `/start` при открытии
- После отправки команды Web App закрывается
- Пользователь видит сообщение от бота с кнопками (Меню, Мои заказы, Статус заказа, Помощь)

