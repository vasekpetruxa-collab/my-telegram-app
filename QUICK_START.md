# 🚀 Быстрый запуск бота с логами

## 📋 Команды для PowerShell (копируйте БЕЗ обратных кавычек):

### Шаг 1: Перейти в папку проекта
```
cd d:\my-telegram-app
```

### Шаг 2: Обновить PATH (если node не найден)
```
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
```

### Шаг 3: Запустить бота
```
node bot.js
```

## ⚠️ ВАЖНО:

- **НЕ копируйте обратные кавычки** ``` - это только форматирование для документации
- **Копируйте только текст внутри** блоков кода
- **Выполняйте команды по порядку** в PowerShell

## ✅ Пример правильного копирования:

**НЕПРАВИЛЬНО:**
```
```powershell
cd d:\my-telegram-app
```
```

**ПРАВИЛЬНО:**
```
cd d:\my-telegram-app
```

## 📝 Полная последовательность команд:

1. Откройте PowerShell
2. Скопируйте и выполните (БЕЗ обратных кавычек):
   ```
   cd d:\my-telegram-app
   ```
3. Затем:
   ```
   node bot.js
   ```

## 🔍 Если node не найден:

Используйте полный путь:
```
D:\node.exe bot.js
```

Или обновите PATH:
```
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
node bot.js
```

