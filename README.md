# 🍕 Telegram Web App - Меню пиццерии

Telegram Web App для заказа еды из пиццерии "У Итальянца". Приложение позволяет просматривать меню, добавлять блюда в корзину и оформлять заказы прямо в Telegram.

## ✨ Возможности

- 📱 **Адаптивный дизайн** - отлично работает на мобильных устройствах
- 🔍 **Поиск по меню** - быстрый поиск блюд по названию и описанию
- 🛒 **Корзина покупок** - добавление, изменение количества и удаление товаров
- 💾 **Сохранение корзины** - корзина сохраняется в localStorage и восстанавливается при перезагрузке
- 🎨 **Современный UI** - красивые анимации и плавные переходы
- 🔔 **Уведомления** - toast-уведомления о действиях пользователя
- ⚡ **Быстрая работа** - оптимизированный код без лишних зависимостей

## 🚀 Установка и запуск

1. Клонируйте репозиторий или скачайте файлы
2. Откройте `index.html` в браузере или разместите файлы на веб-сервере
3. Для работы в Telegram создайте бота через [@BotFather](https://t.me/BotFather) и настройте Web App

## 📁 Структура проекта

```
my-telegram-app/
├── index.html      # Основная HTML-разметка
├── app.js          # Логика приложения
├── data.js         # Данные меню
├── style.css       # Стили и анимации
└── README.md       # Документация
```

## 🛠 Технологии

- Vanilla JavaScript (без фреймворков)
- Telegram Web App API
- CSS3 с анимациями
- LocalStorage для сохранения данных

## 📝 Использование

1. **Просмотр меню** - прокрутите список блюд или выберите категорию
2. **Поиск** - введите название блюда в поле поиска
3. **Добавление в корзину** - нажмите кнопку "+" на карточке блюда
4. **Управление корзиной** - откройте корзину, измените количество или удалите товары
5. **Оформление заказа** - нажмите «Далее» в корзине, заполните данные и отправьте заказ

## 🔧 Настройка

Для изменения меню отредактируйте файл `data.js`:

```javascript
const menuData = {
    restaurant: {
        name: "Название ресторана",
        description: "Описание"
    },
    categories: [...],
    items: [...]
};
```

## 📱 Интеграция с Telegram

Приложение использует Telegram Web App API для:
- Отправки данных заказа в бота
- Отображения кнопки оформления заказа
- Получения информации о пользователе
- Показывания popup-уведомлений

### CRM и обработка web_app_data

После вызова `tg.sendData(orderPayload)` бот получает объект `web_app_data` и может сохранить заказ в CRM. Пример на Node.js (Telegraf):

```js
bot.on('message', async (ctx) => {
    const payload = ctx.message?.web_app_data?.data;
    if (!payload) return;

    const order = JSON.parse(payload);
    // order включает phone, address, cutlery, deliveryType и т.д.
    await crmClient.createDeal({
        customerPhone: order.phone,
        customerAddress: order.address,
        items: order.items,
        total: order.total,
        deliveryType: order.deliveryType,
        cutlery: order.cutlery,
        paymentMethod: order.paymentMethod,
        recipientName: order.recipientName
    });

    await ctx.reply('Заказ принят! Мы уже готовим ваше блюдо 🍕');
});
```

> В CRM можно передать любые дополнительные поля — просто добавьте их в объект `orderData` внутри `sendOrderData()` и обработайте на стороне бота.

## 🐛 Обработка ошибок

Приложение включает:
- Проверку наличия Telegram WebApp API
- Обработку ошибок при оформлении заказа
- Валидацию данных корзины
- Fallback режим для разработки вне Telegram

## 📄 Лицензия

Свободное использование для личных и коммерческих проектов.
