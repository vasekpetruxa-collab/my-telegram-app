# 🚀 Быстрая настройка проекта

## Шаг 1: Установка Node.js

1. Скачайте Node.js с официального сайта: https://nodejs.org/
2. Выберите версию **LTS** (рекомендуется)
3. Установите, следуя инструкциям установщика
4. **Перезапустите терминал** после установки

## Шаг 2: Установка зависимостей

После установки Node.js выполните в терминале:

```bash
npm install
```

Эта команда установит все необходимые пакеты из `package.json`:
- `telegraf` - библиотека для работы с Telegram Bot API
- `dotenv` - для загрузки переменных из `.env` файла

## Шаг 3: Проверка .env файла

Ваш `.env` файл уже создан и содержит:
- `BOT_TOKEN` - токен бота
- `WEB_APP_URL` - URL вашего Web App
- `ADMIN_IDS` - ID администраторов

**Важно:** Файл `.env` автоматически загружается при запуске бота через `dotenv` (уже настроено в `bot.js`).

## Шаг 4: Запуск бота

После установки зависимостей запустите бота:

```bash
npm start
```

Или для разработки с автоперезагрузкой:

```bash
npm run dev
```

## ✅ Готово!

Если все настроено правильно, вы увидите:
```
🤖 Бот запущен и готов к работе!
📱 Web App URL: https://github.com/vasekpetruxa-collab/my-telegram-app.git
```

---

**Примечание:** Убедитесь, что:
- Node.js установлен (проверьте: `node --version`)
- npm установлен (проверьте: `npm --version`)
- Файл `.env` содержит правильные значения

