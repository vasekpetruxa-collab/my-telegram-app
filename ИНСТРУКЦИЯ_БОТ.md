# 🤖 Как редактировать и добавлять функции в Telegram боте

## 📍 Где находится код бота?

Код бота находится в файле **`bot.js`** в корне проекта.

## 🚀 Быстрый старт

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Создайте файл `.env`** (скопируйте из `env.example`):
   ```
   BOT_TOKEN=ваш_токен_от_BotFather
   WEB_APP_URL=https://ваш-сайт.com
   ADMIN_IDS=ваш_telegram_id
   ```

3. **Запустите бота:**
   ```bash
   npm start
   ```

## ✏️ Как добавить новую команду

Откройте `bot.js` и найдите раздел "ОСНОВНЫЕ КОМАНДЫ БОТА". Добавьте новую команду:

```javascript
// Команда /mycommand - описание
bot.command('mycommand', async (ctx) => {
    await ctx.reply('Привет! Это моя новая команда!');
});
```

**Пример:** Добавим команду `/info`:

```javascript
bot.command('info', async (ctx) => {
    const info = `
ℹ️ Информация о ресторане:

📍 Адрес: г. Шахты, ул. Советская, дом 235 «Бункер»
🕐 Время работы: 12:00 - 24:00
📞 Телефон: +7 (999) 123-45-67
    `;
    await ctx.reply(info);
});
```

## 🎯 Как обработать текстовые сообщения

Добавьте обработчик текста после команд:

```javascript
// Обработка текстовых сообщений
bot.on('text', async (ctx) => {
    const text = ctx.message.text.toLowerCase();
    
    if (text.includes('привет') || text.includes('здравствуй')) {
        await ctx.reply('Привет! Чем могу помочь? Используйте /menu для просмотра меню.');
    } else if (text.includes('адрес')) {
        await ctx.reply('📍 Наш адрес: г. Шахты, ул. Советская, дом 235 «Бункер»');
    } else if (text.includes('время')) {
        await ctx.reply('🕐 Мы работаем с 12:00 до 24:00 ежедневно');
    }
});
```

## 🔘 Как добавить кнопки

Используйте `reply_markup` для создания кнопок:

```javascript
bot.command('contacts', async (ctx) => {
    await ctx.reply('Наши контакты:', {
        reply_markup: {
            inline_keyboard: [
                [
                    { text: '📞 Позвонить', url: 'tel:+79991234567' },
                    { text: '📍 Адрес', callback_data: 'show_address' }
                ],
                [
                    { text: '🍕 Открыть меню', web_app: { url: process.env.WEB_APP_URL } }
                ]
            ]
        }
    });
});

// Обработка нажатий на кнопки
bot.action('show_address', async (ctx) => {
    await ctx.answerCbQuery(); // Убираем "часики" у кнопки
    await ctx.reply('📍 Адрес: г. Шахты, ул. Советская, дом 235 «Бункер»');
});
```

## 📦 Как изменить обработку заказов

Найдите функцию обработки заказов (около строки 118):

```javascript
bot.on('message', async (ctx) => {
    const webAppData = ctx.message?.web_app?.data || ctx.message?.web_app_data?.data;
    
    if (!webAppData) return;
    
    try {
        const orderData = JSON.parse(webAppData);
        
        // ВОТ ЗДЕСЬ ВЫ МОЖЕТЕ ДОБАВИТЬ СВОЮ ЛОГИКУ:
        
        // 1. Сохранение в базу данных
        // await saveToDatabase(orderData);
        
        // 2. Отправка в CRM
        // await sendToCRM(orderData);
        
        // 3. Отправка email уведомления
        // await sendEmailNotification(orderData);
        
        // 4. Изменение формата сообщения
        // const customMessage = formatCustomMessage(orderData);
        // await ctx.reply(customMessage);
        
    } catch (error) {
        console.error('Ошибка:', error);
    }
});
```

## 🔔 Как добавить уведомления администраторам

Функция `notifyAdmins` уже есть в коде. Вы можете изменить формат уведомления:

```javascript
async function notifyAdmins(ctx, order) {
    const adminIds = (process.env.ADMIN_IDS || '').split(',').map(id => parseInt(id.trim()));
    
    // Ваш кастомный формат сообщения
    const adminMessage = `
🔔 НОВЫЙ ЗАКАЗ #${order.orderId}

Клиент: ${order.recipientName}
Телефон: ${order.phone}
Сумма: ${order.total} ₽
    `;
    
    for (const adminId of adminIds) {
        await bot.telegram.sendMessage(adminId, adminMessage);
    }
}
```

## 💾 Как добавить базу данных

### Пример с MongoDB:

1. Установите библиотеку:
   ```bash
   npm install mongoose
   ```

2. Добавьте в начало `bot.js`:
   ```javascript
   const mongoose = require('mongoose');
   mongoose.connect(process.env.MONGODB_URI);
   
   const orderSchema = new mongoose.Schema({
       orderId: String,
       items: Array,
       total: Number,
       phone: String,
       // ... другие поля
   });
   
   const Order = mongoose.model('Order', orderSchema);
   ```

3. Сохраняйте заказы:
   ```javascript
   const order = new Order(orderData);
   await order.save();
   ```

## 🔄 Как изменить формат сообщения о заказе

Найдите функцию `formatOrderMessage` (около строки 150) и измените её:

```javascript
function formatOrderMessage(order) {
    // Ваш кастомный формат
    return `
✅ Заказ принят!
Номер: ${order.orderId}
Сумма: ${order.total} ₽
    `;
}
```

## 📝 Полезные примеры

### Отправка фото

```javascript
bot.command('photo', async (ctx) => {
    await ctx.replyWithPhoto({ 
        source: './images/restaurant.jpg' 
    }, {
        caption: 'Фото нашего ресторана'
    });
});
```

### Отправка локации

```javascript
bot.command('location', async (ctx) => {
    await ctx.replyWithLocation(47.7085, 40.2144, {
        // Координаты ресторана
    });
});
```

### Обработка callback_query (нажатия на кнопки)

```javascript
bot.action('button_clicked', async (ctx) => {
    await ctx.answerCbQuery('Кнопка нажата!');
    await ctx.reply('Вы нажали на кнопку!');
});
```

## 🐛 Отладка

Для отладки используйте `console.log`:

```javascript
bot.on('message', async (ctx) => {
    console.log('Получено сообщение:', ctx.message);
    // Ваш код
});
```

## 📚 Дополнительные ресурсы

- [Документация Telegraf](https://telegraf.js.org/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- Подробная инструкция: [BOT_SETUP.md](BOT_SETUP.md)

## ⚠️ Важно

После изменения кода бота:
1. Остановите бота (Ctrl+C)
2. Запустите снова: `npm start`

Или используйте `npm run dev` для автоматической перезагрузки при изменениях.

